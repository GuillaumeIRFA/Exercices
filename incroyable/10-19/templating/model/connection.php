<?php

$host = "localhost";
$user = "Films";
$password = "coquelicot28";
$database = "templating";

$connection = mysqli_connect($host, $user, $password, $database);

// if($connection) { echo "Connected"; } else { echo "Failed to connect"; }

?>